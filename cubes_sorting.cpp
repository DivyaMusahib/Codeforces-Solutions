#include<bits/stdc++.h>
using namespace std;

int main(){
    int t; cin >> t;

    while(t--){
        int n; cin >> n;

        vector<int>a(n);
        //#new method iterator + auto
        for(auto it:a){
            cin >> it;
        }
        for(int i=0; i<n; i++){
            
        }
    }
}