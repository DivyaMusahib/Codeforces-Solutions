#include "bits/stdc++.h"
using namespace std;

int main(){
    int t;
    cin >> t;
    while(t--){
        int length, breadth, i, j;
        cin >> length >> breadth >> i >> j;

        cout << 1 << " " << 1 << " "; // put ball in top right
        cout << length << " " << breadth << "\n"; // put ball in bottom left
    }
}