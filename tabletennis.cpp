#include<bits/stdc++.h>
using namespace std;

int main(){
    long long n,k;
    cin >>n >> k;

    queue<pair<long long , long long>>q;
    // first is power second is number of match played
    long long p;
    for(int i=0; i<n; i++){
        cin >> p;
        q.push({p,0});
    }

    long long max = 0;
    while(max==k){
        long long temp 
    }


}