#include<bits/stdc++.h>
using namespace std;

int main(){
    int t; cin >> t;
    while(t--){
        int n; cin >> n;
        vector<char>s(n);

        for(int i=0; i<n; i++){
            cin >> s[i];
        }

        //cv cvc
        // int j=n-1;
        // while(j>=1){
        //     if((s[j]=='b' || s[j]=='c' || s[j]=='d') && (s[j-1]=='a' || s[j-1]=='e') && (s[j-2]=='b' || s[j-2]=='c' || s[j-2]=='d')){
        //         s.insert(j-2,'.');
        //     }
        // }
        
        

    }
}